ssh:
  hosts:
    # -------------------------------------------------------------------------
    # Applied on every machine regardless of OS or context
    # -------------------------------------------------------------------------
    common:
      - name: "github.com"
        comment: "GitHub – always use SSH key auth"
        options:
          User: git

    # -------------------------------------------------------------------------
    # macOS (darwin) only
    # -------------------------------------------------------------------------
    darwin: []
    #   - name: "*.local"
    #     comment: "mDNS hosts on the local LAN"
    #     options:
    #       StrictHostKeyChecking: "no"
    #       CheckHostIP: "no"

    # -------------------------------------------------------------------------
    # Bare-metal Linux only (not WSL)
    # -------------------------------------------------------------------------
    linux: []

    # -------------------------------------------------------------------------
    # WSL / GNU Linux only
    # -------------------------------------------------------------------------
    gnu_linux: []

    # -------------------------------------------------------------------------
    # Windows only
    # -------------------------------------------------------------------------
    windows: []

    # -------------------------------------------------------------------------
    # Personal machines
    # -------------------------------------------------------------------------
    personal:
      - name: "192.168.100.1"
        comment: "Gateway – password auth, no 1Password agent (keys not configured on device)"
        options:
          User: "root"
          IdentityAgent: "none"
          PreferredAuthentications: "password,keyboard-interactive"
          PubkeyAuthentication: "no"

      - name: "qnap qnap.local 192.168.1.15 192.168.1.14"
        comment: "Qnap Admin SSH"
        options:
          HostName: "192.168.1.15"
          User: "qnap-admin"

      # - name: "homelab"
      #   comment: "Home server"
      #   options:
      #     HostName: "192.168.1.1"
      #     User: braydon
      #     IdentityFile: "~/.ssh/id_ed25519"

    # -------------------------------------------------------------------------
    # Work machines
    # -------------------------------------------------------------------------
    work:
      []
      # - name: "bastion"
      #   comment: "Work jump host"
      #   options:
      #     HostName: "bastion.example.com"
      #     User: bbrinkerhoff
      #     IdentityFile: "~/.ssh/id_ed25519_work"
